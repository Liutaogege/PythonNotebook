```python
	验证配置是否正确: nginx  -t
  查看Nginx的详细的版本号：nginx  -V
  查看Nginx的简洁版本号：nginx  -v
  启动Nginx：start  nginx或者直接nginx
  快速停止或关闭Nginx：nginx   -s   stop
  正常停止或关闭Nginx：nginx   -s   quit
  配置文件修改重装载命令：nginx   -s  reload	
	查看nginx进程: ps aux | grep nginx
```

